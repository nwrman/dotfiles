Host *
    # UseKeychain is macOS-only. IgnoreUnknown lets the same config load
    # without errors on Linux/WSL and Windows OpenSSH.
    IgnoreUnknown UseKeychain
    AddKeysToAgent yes
    UseKeychain yes
    IdentityFile ~/.ssh/id_ed25519
